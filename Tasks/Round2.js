function E2A(str){
    if(str.match(/$[0-9]/)){
       let encodeObj = {};
    for(let item =0; item< str.length; item++){
        
    }
    }else{
        
    function Simplfy(char){
        //let strObj = {},decodStr = '', 
        let count=0; 

        for(let item in str){
            // if(strObj.hasOwnProperty(char)){
            //     strObj[char] += 1
            // }else{
            //     strObj[char] = 1
            // }
            if(item === char)
                count++
        }
        return count;
    
        // for(item in strObj){
        //     decodStr +=  item+strObj[item] 
        // }
    
        // return decodStr;
    }

for(let i in  str){
    let encodedStr = ''
    encodedStr += i+ Simplfy(i);
}
    }
}

//str = 'aaabbaaa'

a(1)(2)